#include <stdio.h>
#pragma warning(disable:4996)

int main(void)
{
	long long N;
	long long i;
	long long sum = 0;

	scanf("%d", &N);

	for (i = 1; ; i++)
	{
		sum += i;
		if (sum > N)
		{
			break;
		}
	}

	printf("%ld", i-1);




	return 0;
}

이거 비쥬얼은 안되는데 백준은 된다
왜지?


